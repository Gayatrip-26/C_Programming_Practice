#include <stdio.h>
 void main ()
     {
        int a, b, c;
           printf("Enter any two numbers \n");
           scanf("%d,%d,%d",&a,&b,&c);

        if(a>b)
          {
            if
             printf("%d is large no",a);

            else
             printf("%d is large no",b);

          }

         if else(b>c)
          {
             if
              printf("%d is large no",b);

            else 
             printf("%d is large no",c);

          }

         else(c>a)
          {
            if
             printf("%d is large no",c);

            else 
             printf("%d is large no",a);

          }
          
     }