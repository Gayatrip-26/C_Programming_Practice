#include <stdio.h>
 void main()
    { 
      float area,redius;
           printf("Enter any redius of a circle :\n");
           scanf("%f",&redius );

           area=3.14*(redius*redius);

           printf("The area of a circle is %f",area );

           return 0;
    }